# Generated: node compile.mjs / npx vite build web. Whether a compiled
# .pancake gets committed as the golden conformance fixture is an open
# decision (spec/COMPLETE_PROFILE.md section 5).
*.pancake
*.pikelet
*.tampered
web/dist/

# web/public/veyra*.pikelet used to be a committed exception to the
# *.pikelet rule above (~75MB across 3 files). They're now release assets,
# fetched and SHA-256-verified by web/veyra-corpus/fetch-veyra.mjs — see
# that script and web/veyra-corpus/README.md for the source and rebuild
# recipe. No exception needed here anymore; they're gitignored like any
# other compiled .pikelet output.

student-pilot/
encoder-spike/spike.mjs
encoder-spike/spike.wasm
encoder-spike/real/
