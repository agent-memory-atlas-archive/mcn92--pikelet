{
  "name": "pikelet-native",
  "version": "0.1.0",
  "private": true,
  "description": "Native N-API binding for the Pikelet HNSW engine (SSE2 SIMD)",
  "main": "index.js",
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "test": "node test.js && npm run test:compact",
    "test:compact": "g++ -std=c++17 -O0 -Wall -I../src compact_test.cpp -o /tmp/pikelet_compact_test && /tmp/pikelet_compact_test"
  },
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "node-gyp": "^12.4.0"
  }
}
